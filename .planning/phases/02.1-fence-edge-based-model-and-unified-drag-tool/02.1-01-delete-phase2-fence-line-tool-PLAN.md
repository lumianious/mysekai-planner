---
phase: 02.1-fence-edge-based-model-and-unified-drag-tool
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useFenceLineTool.ts
  - src/components/canvas/FenceLineTool.tsx
  - src/components/canvas/FenceConfirmOverlay.tsx
  - src/__tests__/fenceLineTool.test.ts
  - src/components/canvas/EditorCanvas.tsx
  - src/hooks/useKeyboard.ts
  - src/data/fixtures.ts
  - src/__tests__/groundSubtype.test.ts
autonomous: true
requirements: [ROAD-03]
must_haves:
  truths:
    - "All four Phase 2 fence line-tool source files are removed from the tree"
    - "EditorCanvas.tsx has no references to useFenceLineTool, FenceLineTool, or FenceConfirmOverlay"
    - "useKeyboard.ts no longer accepts or wires onFenceConfirm / onFenceCancel"
    - "getBrushInteraction returns 'drag-paint-edge' for fences (not 'line-tool')"
    - "pnpm exec tsc --noEmit succeeds (no stale references)"
    - "pnpm test --run passes (minus the deleted fence line tool test)"
  artifacts:
    - path: "src/data/fixtures.ts"
      provides: "Brush interaction discriminator returning 'drag-paint' | 'drag-paint-edge' | null"
      contains: "drag-paint-edge"
    - path: "src/components/canvas/EditorCanvas.tsx"
      provides: "Canvas surface with all fence-line-tool imports/JSX removed"
    - path: "src/hooks/useKeyboard.ts"
      provides: "Keyboard hook without fence confirm/cancel wiring"
    - path: "src/__tests__/groundSubtype.test.ts"
      provides: "Updated assertion for new 'drag-paint-edge' return value"
  key_links:
    - from: "src/data/fixtures.ts"
      to: "all call sites of getBrushInteraction"
      via: "discriminated union value 'drag-paint-edge'"
      pattern: "drag-paint-edge"
    - from: "src/__tests__/groundSubtype.test.ts"
      to: "getBrushInteraction"
      via: "assertion expecting 'drag-paint-edge'"
      pattern: "drag-paint-edge"
---

<objective>
Tear out the Phase 2 fence click-line tool surface area and rename the brush-interaction discriminator from `'line-tool'` to `'drag-paint-edge'`. This creates a clean compile-time foundation for the new edge data model and unified drag handler (Plans 02 and 03) to land on.

Purpose: Delete-don't-refactor per research decision. Phase 2's click-line UX is architecturally wrong; no transitional code should survive. The rename gives us a type-system safety net — every switch/conditional that used to branch on `'line-tool'` will break loudly until Plan 03 updates it.

Output: 4 files deleted, 4 files modified, tree compiles, test suite green (sans the deleted fence line tool test).

Addresses: ROAD-03 (corrected) — first step.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-fence-edge-based-model-and-unified-drag-tool/02.1-RESEARCH.md

@src/hooks/useFenceLineTool.ts
@src/components/canvas/FenceLineTool.tsx
@src/components/canvas/FenceConfirmOverlay.tsx
@src/__tests__/fenceLineTool.test.ts
@src/components/canvas/EditorCanvas.tsx
@src/hooks/useKeyboard.ts
@src/data/fixtures.ts
@src/__tests__/groundSubtype.test.ts
@src/components/canvas/GhostPreview.tsx

<interfaces>
<!-- Critical: the brush-interaction discriminator is the single choke point for routing -->
<!-- Current (from src/data/fixtures.ts): -->
```typescript
export function getBrushInteraction(fixture: Fixture): 'drag-paint' | 'line-tool' | null
```
<!-- After this plan: -->
```typescript
export function getBrushInteraction(fixture: Fixture): 'drag-paint' | 'drag-paint-edge' | null
```

<!-- GhostPreview.tsx L60-62 (approx) currently hides the tile ghost when interaction === 'line-tool'.
     That logic stays — just update the literal to 'drag-paint-edge'. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the four Phase 2 fence line-tool files</name>
  <files>
    src/hooks/useFenceLineTool.ts (DELETE)
    src/components/canvas/FenceLineTool.tsx (DELETE)
    src/components/canvas/FenceConfirmOverlay.tsx (DELETE)
    src/__tests__/fenceLineTool.test.ts (DELETE)
  </files>
  <read_first>
    - src/hooks/useFenceLineTool.ts — confirm this is the Phase 2 three-phase state-machine hook (not something unrelated)
    - src/components/canvas/FenceLineTool.tsx — confirm it is the Konva ghost preview layer for the line tool
    - src/components/canvas/FenceConfirmOverlay.tsx — confirm it is the HTML confirm/cancel overlay
    - src/__tests__/fenceLineTool.test.ts — confirm it tests the line-tool state machine (obsolete; new behavioral tests are co-located in Plans 02 and 03 Task 1 per VALIDATION.md)
  </read_first>
  <action>
    Delete these four files exactly:
    1. `rm src/hooks/useFenceLineTool.ts`
    2. `rm src/components/canvas/FenceLineTool.tsx`
    3. `rm src/components/canvas/FenceConfirmOverlay.tsx`
    4. `rm src/__tests__/fenceLineTool.test.ts`

    Do NOT create any replacement files in this task. Do NOT touch any other file. The new behavioral tests `edgeRasterize.test.ts` and `fenceEdgeBrush.test.ts` are co-located in Plan 02 Task 1 and Plan 03 Task 1 respectively (per VALIDATION.md) — not created here.

    Note: Deleting source files breaks the TypeScript build until Task 2 removes the import references — that's expected. Do not run `tsc` between Task 1 and Task 2.

    Per CLAUDE.md Scope Discipline: do NOT modify anything else in this task.
  </action>
  <verify>
    <automated>test ! -f src/hooks/useFenceLineTool.ts && test ! -f src/components/canvas/FenceLineTool.tsx && test ! -f src/components/canvas/FenceConfirmOverlay.tsx && test ! -f src/__tests__/fenceLineTool.test.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `ls src/hooks/useFenceLineTool.ts` → "No such file"
    - `ls src/components/canvas/FenceLineTool.tsx` → "No such file"
    - `ls src/components/canvas/FenceConfirmOverlay.tsx` → "No such file"
    - `ls src/__tests__/fenceLineTool.test.ts` → "No such file"
    - No other files modified by this task (git diff --stat shows exactly 4 deletions, 0 modifications)
  </acceptance_criteria>
  <done>Four files are gone from the working tree. No replacements created.</done>
</task>

<task type="auto">
  <name>Task 2: Strip fence line-tool references from EditorCanvas.tsx and useKeyboard.ts</name>
  <files>
    src/components/canvas/EditorCanvas.tsx
    src/hooks/useKeyboard.ts
  </files>
  <read_first>
    - src/components/canvas/EditorCanvas.tsx (ENTIRE FILE — 582 lines) — you must understand every location that touches the fence line tool
    - src/hooks/useKeyboard.ts (ENTIRE FILE) — confirm onFenceConfirm / onFenceCancel wiring locations
    - .planning/phases/02.1-fence-edge-based-model-and-unified-drag-tool/02.1-RESEARCH.md section "Files to MODIFY" (rows for EditorCanvas.tsx and useKeyboard.ts) — exact locations listed
  </read_first>
  <action>
    === EditorCanvas.tsx ===

    1. Remove these imports (exact matches at top of file):
       - `import { useFenceLineTool } from '...'`
       - `import { FenceLineTool } from '...'`
       - `import { FenceConfirmOverlay } from '...'`

    2. Remove the `useFenceLineTool(...)` hook call and its destructured return (the hook returned at least: `fenceState`, `handleFenceClick`, `handleFenceMouseMove`, `confirmFenceLine`, `cancelFenceLine`). Delete the entire hook invocation block.

    3. In `handleStageClick`, remove the fence-click branch (research notes it is near L465-470, begins with something like `if (interaction === 'line-tool')` or `if (fenceState...)`). Leave the rest of `handleStageClick` intact.

    4. In `handleStageMouseDown`, remove the comment/guard that currently SKIPS mousedown for fence fixtures (research notes near L305: the "skip mousedown for fences" early return). After this plan, the existing `'drag-paint'` branch still runs; Plan 03 will ADD a sibling `'drag-paint-edge'` branch. DO NOT ADD THE NEW BRANCH IN THIS PLAN.

    5. In `handleMouseMove`, remove the delegation to `handleFenceMouseMove` (research notes near L348). The remaining body must still handle `isPaintingRef` / `isErasingRef` correctly (unchanged logic).

    6. In JSX, remove:
       - `<FenceLineTool ... />` block (near L548)
       - `<FenceConfirmOverlay ... />` block (near L562)

    7. Remove any local state/refs that existed SOLELY to support the fence line tool (e.g., anything named `fenceState`, `fenceAnchor`). Keep all other refs untouched.

    8. Remove any `onFenceConfirm={...}` / `onFenceCancel={...}` props passed into `useKeyboard(...)`.

    === useKeyboard.ts ===

    9. Remove `onFenceConfirm` and `onFenceCancel` from the options/params type.
    10. Remove the Enter-key handler entirely (it only existed for fence confirm per research L17-22, L38-43, L59-61). If the Enter handler has other responsibilities, leave those and ONLY remove the fence branch — read the file carefully first.
    11. Simplify the Escape handler: remove the fence-cancel branch so Escape falls through to the standard deselect / clear-activeFixture behavior that already exists.

    === Constraints ===

    - Do NOT introduce `isEdgePaintingRef`, `edgeStrokeAxisRef`, `paintEdgeStroke`, or any new fence handling in this plan — those are Plan 03's job.
    - Do NOT delete road/color-tile drag-paint logic. It must stay byte-identical.
    - Comments: if adding any new explanatory comments, write them in Chinese per CLAUDE.md (with ASCII separator lines if substantial).

    After edits, run `grep -rn "useFenceLineTool\\|FenceLineTool\\|FenceConfirmOverlay\\|fenceState\\|onFenceConfirm\\|onFenceCancel\\|handleFenceClick\\|handleFenceMouseMove\\|confirmFenceLine\\|cancelFenceLine" src/` and confirm ZERO matches before considering the task done.
  </action>
  <verify>
    <automated>grep -rn "useFenceLineTool\|FenceLineTool\|FenceConfirmOverlay\|fenceState\|onFenceConfirm\|onFenceCancel\|handleFenceClick\|handleFenceMouseMove\|confirmFenceLine\|cancelFenceLine" src/ ; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - Zero grep matches for: useFenceLineTool, FenceLineTool, FenceConfirmOverlay, fenceState, onFenceConfirm, onFenceCancel, handleFenceClick, handleFenceMouseMove, confirmFenceLine, cancelFenceLine under `src/`
    - `src/components/canvas/EditorCanvas.tsx` still contains the string `'drag-paint'` (tile brush path preserved)
    - `src/components/canvas/EditorCanvas.tsx` still contains `isPaintingRef` and `isErasingRef` (tile paint/erase refs preserved)
    - `src/hooks/useKeyboard.ts` no longer has a parameter named `onFenceConfirm` or `onFenceCancel`
    - File size of EditorCanvas.tsx is strictly less than the pre-edit size (work was deletion only)
  </acceptance_criteria>
  <done>
    EditorCanvas.tsx and useKeyboard.ts contain zero references to the deleted fence line tool surface. Tile brush path (roads/color-tiles) untouched.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Rename 'line-tool' → 'drag-paint-edge' and repair downstream call sites</name>
  <files>
    src/data/fixtures.ts
    src/__tests__/groundSubtype.test.ts
    src/components/canvas/GhostPreview.tsx
    src/stores/editorStore.ts (if pickToolModeForFixture references 'line-tool')
  </files>
  <behavior>
    - Test: `getBrushInteraction(fenceFixture)` returns `'drag-paint-edge'` (not `'line-tool'`).
    - Test: `getBrushInteraction(roadFixture)` returns `'drag-paint'` (unchanged).
    - Test: `getBrushInteraction(rugFixture)` returns `null` (unchanged, rugs use place flow).
    - Test: `pnpm exec tsc --noEmit` succeeds — every switch/conditional that used `'line-tool'` now uses `'drag-paint-edge'` or is gone.
  </behavior>
  <read_first>
    - src/data/fixtures.ts (ENTIRE FILE) — find `getBrushInteraction` implementation and return type
    - src/__tests__/groundSubtype.test.ts L143-144 and surrounding context — the assertion the research calls out
    - src/components/canvas/GhostPreview.tsx L55-65 — the research says GhostPreview hides the tile ghost for `'line-tool'` fixtures (L60-62); update the literal
    - src/stores/editorStore.ts — search for `line-tool` (research mentions `pickToolModeForFixture` near L47 may reference it)
    - Run `grep -rn "'line-tool'" src/` BEFORE editing to enumerate every occurrence
  </read_first>
  <action>
    1. Run `grep -rn "'line-tool'" src/` and list every occurrence. There should be approximately 3-5: `fixtures.ts` (return type + return statement), `groundSubtype.test.ts` (assertion), `GhostPreview.tsx` (conditional), possibly `editorStore.ts` (pickToolModeForFixture).

    2. In `src/data/fixtures.ts`:
       - Change the return type union of `getBrushInteraction` from `'drag-paint' | 'line-tool' | null` to `'drag-paint' | 'drag-paint-edge' | null`.
       - Change the return value for fence fixtures from `'line-tool'` to `'drag-paint-edge'`.
       - Update any JSDoc / Chinese comment headers to reflect the new semantic: drag-paint targets tiles, drag-paint-edge targets grid edges.

    3. In `src/__tests__/groundSubtype.test.ts` (~L143-144):
       - Update the assertion from `expect(getBrushInteraction(fence)).toBe('line-tool')` to `expect(getBrushInteraction(fence)).toBe('drag-paint-edge')`.

    4. In `src/components/canvas/GhostPreview.tsx` (~L55-65):
       - Replace the literal `'line-tool'` with `'drag-paint-edge'` in the conditional that hides the tile ghost.
       - DO NOT change any other logic. GhostPreview still hides itself for edge fixtures; Plan 04's FenceLayer handles the edge visual.

    5. In `src/stores/editorStore.ts`:
       - Search for `line-tool`. If `pickToolModeForFixture` (or any other function) uses it, replace with `'drag-paint-edge'`. If the function only cares "is this a brush fixture" via `!== null`, no rename needed — leave it.

    6. Run `grep -rn "'line-tool'" src/` → must return ZERO matches.

    7. Run `pnpm exec tsc --noEmit` → must succeed. If any file still imports deleted symbols, fix by removing (scope: only the import line).

    8. Run `pnpm test --run src/__tests__/groundSubtype.test.ts src/__tests__/setActiveFixture.test.ts src/__tests__/temporalBatch.test.ts` → all three must pass. These validate the rename didn't break the remaining tile brush and ground subtype classification.

    9. Per CLAUDE.md DocOps: if any L3 file header in the edited files references `'line-tool'`, update it.
  </action>
  <verify>
    <automated>grep -rn "'line-tool'" src/ ; test $? -eq 1 && pnpm exec tsc --noEmit && pnpm test --run src/__tests__/groundSubtype.test.ts src/__tests__/setActiveFixture.test.ts src/__tests__/temporalBatch.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "'line-tool'" src/` returns zero matches
    - `pnpm exec tsc --noEmit` exits 0
    - `src/data/fixtures.ts` contains the literal `'drag-paint-edge'` in the `getBrushInteraction` return statement
    - `src/data/fixtures.ts` return type union of `getBrushInteraction` is exactly `'drag-paint' | 'drag-paint-edge' | null`
    - `src/__tests__/groundSubtype.test.ts` contains `'drag-paint-edge'` at the updated assertion line (~L143-144)
    - `pnpm test --run src/__tests__/groundSubtype.test.ts src/__tests__/setActiveFixture.test.ts src/__tests__/temporalBatch.test.ts` exits 0
    - `pnpm test --run` overall exits 0 (minus deleted fenceLineTool.test.ts which no longer exists)
  </acceptance_criteria>
  <done>
    The brush-interaction discriminator is renamed cleanly. Type check passes. Remaining test suite is green. The code is ready for Plan 02 to introduce the edge data model and Plan 03 to wire the new handler branch.
  </done>
</task>

</tasks>

<verification>
After all tasks:

```bash
# 1. Confirm deletion
test ! -f src/hooks/useFenceLineTool.ts
test ! -f src/components/canvas/FenceLineTool.tsx
test ! -f src/components/canvas/FenceConfirmOverlay.tsx
test ! -f src/__tests__/fenceLineTool.test.ts

# 2. Confirm no dangling references
grep -rn "useFenceLineTool\|FenceLineTool\|FenceConfirmOverlay\|fenceState\|onFenceConfirm\|onFenceCancel\|'line-tool'" src/
# Expected: exit 1 (no matches)

# 3. Type check
pnpm exec tsc --noEmit

# 4. Test suite (minus deleted fence line tool)
pnpm test --run
```
</verification>

<success_criteria>
- All 4 target files deleted
- Zero references to deleted symbols or `'line-tool'` literal anywhere in `src/`
- `pnpm exec tsc --noEmit` succeeds
- `pnpm test --run` passes
- `src/data/fixtures.ts` `getBrushInteraction` returns `'drag-paint' | 'drag-paint-edge' | null`
- No road/color-tile drag-paint logic modified (byte-identical outside the fence removals)
- EditorCanvas.tsx is strictly smaller than before this plan
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-fence-edge-based-model-and-unified-drag-tool/02.1-01-SUMMARY.md` describing: files deleted, files modified, line count delta on EditorCanvas.tsx/useKeyboard.ts, and the new discriminator value. Note any unexpected references to the old symbols that needed cleanup beyond the research's enumeration.
</output>
