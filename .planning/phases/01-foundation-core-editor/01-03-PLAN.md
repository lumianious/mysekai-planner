---
phase: 01-foundation-core-editor
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/components/welcome/WelcomeScreen.tsx
  - src/components/toolbar/Toolbar.tsx
  - src/components/toolbar/ToolButton.tsx
  - src/components/hotbar/Hotbar.tsx
  - src/components/status/StatusBar.tsx
  - src/components/layout/EditorLayout.tsx
  - src/App.tsx
autonomous: true
requirements:
  - GRID-02
  - GRID-08
  - GRID-11
must_haves:
  truths:
    - "Welcome screen shows 5 area level cards with correct grid dimensions"
    - "Selecting an area level transitions to the editor layout"
    - "Toolbar shows Select/Move, Stamp, Remove mode buttons with correct icons"
    - "Hotbar shows 9 numbered slots at the bottom"
    - "Status bar shows area level, item count, and zoom level"
    - "Editor layout has left sidebar area, center canvas area, toolbar, hotbar, and status bar"
  artifacts:
    - path: "src/components/welcome/WelcomeScreen.tsx"
      provides: "Area level selection screen"
      min_lines: 40
    - path: "src/components/toolbar/Toolbar.tsx"
      provides: "Top toolbar with tool mode buttons and area selector"
      min_lines: 50
    - path: "src/components/hotbar/Hotbar.tsx"
      provides: "Bottom hotbar with 9 quick-select slots"
      min_lines: 30
    - path: "src/components/status/StatusBar.tsx"
      provides: "Bottom status bar with context info"
      min_lines: 20
    - path: "src/components/layout/EditorLayout.tsx"
      provides: "Main editor layout shell"
      min_lines: 30
  key_links:
    - from: "src/App.tsx"
      to: "src/stores/editorStore.ts"
      via: "reads isEditorReady to toggle Welcome vs Editor"
      pattern: "isEditorReady"
    - from: "src/components/welcome/WelcomeScreen.tsx"
      to: "src/stores/editorStore.ts"
      via: "calls startEditor(level) on card click"
      pattern: "startEditor"
    - from: "src/components/toolbar/Toolbar.tsx"
      to: "src/stores/editorStore.ts"
      via: "reads toolMode, calls setToolMode"
      pattern: "setToolMode|toolMode"
---

<objective>
Build the Welcome Screen (area level selection), the Editor Layout shell (toolbar, sidebar placeholder, canvas placeholder, hotbar, status bar), and all UI chrome components. After this plan, the app boots to a welcome screen, transitions to the editor on level selection, and shows the complete layout structure.

Purpose: Provides the visual shell and navigation flow. Subsequent plans fill in the canvas (Plan 05) and catalog sidebar (Plan 04) within this layout.
Output: Working welcome screen and editor layout with all chrome components rendered.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-core-editor/01-CONTEXT.md
@.planning/phases/01-foundation-core-editor/01-UI-SPEC.md

<interfaces>
<!-- From Plan 01 types — executor must use these -->

From src/types/editor.ts:
```typescript
export type ToolMode = 'select' | 'stamp' | 'remove';
export type AreaLevel = 1 | 2 | 3 | 4 | 5;
export interface HotbarSlot { fixtureId: number | null; }
```

From src/data/areaLevels.ts:
```typescript
export const AREA_LEVELS: Record<AreaLevel, { gridSize: GridSize; label: string }>;
```

Note: The editor store (src/stores/editorStore.ts) is being built in Plan 02 (same wave). If it doesn't exist yet at execution time, create a temporary minimal store stub with just the fields this plan needs (isEditorReady, startEditor, toolMode, setToolMode, areaLevel, overwriteEnabled, toggleOverwrite, hotbar, activateHotbar). The real store from Plan 02 will supersede it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Welcome Screen and Editor Layout Shell</name>
  <files>
    src/components/welcome/WelcomeScreen.tsx,
    src/components/layout/EditorLayout.tsx,
    src/App.tsx
  </files>
  <read_first>
    src/types/editor.ts (AreaLevel type, AREA_LEVELS),
    src/data/areaLevels.ts (AREA_LEVELS constant),
    .planning/phases/01-foundation-core-editor/01-UI-SPEC.md (Welcome Screen Contract section, Layout Contract section, Color section, Typography section, Copywriting Contract)
  </read_first>
  <action>
    1. Create `src/components/welcome/WelcomeScreen.tsx`:

    Full-screen overlay centered on `bg-surface`. Structure per UI-SPEC:
    - Title: "MySekai 区域规划器" — `text-2xl font-semibold text-primary`
    - Subtitle: "我的世界 户外区域布局设计工具" — `text-sm text-muted`
    - 5 level cards arranged in a flex wrap row (3 top, 2 bottom centered):
      - Each card: `w-32 h-24 rounded-lg bg-surface-raised border border-default hover:border-accent cursor-pointer`
      - Level label: "{AREA_LEVELS[level].label}" — `text-lg font-semibold text-primary`
      - Grid size: "{width}×{depth}" — `text-sm text-muted`
    - Prompt: "选择区域等级开始编辑" — `text-sm text-muted`
    - On card click: call `startEditor(level)` from editor store

    Iterate over `Object.entries(AREA_LEVELS)` to render cards. Use `gap-4` between cards, `gap-6` between sections.

    2. Create `src/components/layout/EditorLayout.tsx`:

    The main editor layout per UI-SPEC Layout Contract:
    ```
    Toolbar (h-10, bg-surface-raised, full width)
    ├── Sidebar (w-72, bg-surface-raised) + Canvas area (flex-1, bg-surface)
    Hotbar (h-14, bg-surface-raised, full width)
    StatusBar (h-8, bg-surface-raised, full width)
    ```

    Structure as a flex column (`h-screen`):
    - `<Toolbar />` — fixed height `h-10`
    - Middle row: flex row containing `<CatalogSidebar />` (placeholder div with `w-72 bg-surface-raised border-r border-default` for now) + canvas area (`flex-1 bg-surface` — placeholder div with centered text "从左侧目录选择家具开始布局")
    - `<Hotbar />` — fixed height `h-14`
    - `<StatusBar />` — fixed height `h-8`

    3. Update `src/App.tsx`:
    - Read `isEditorReady` from editor store (or the stub if store not yet available)
    - If `!isEditorReady`: render `<WelcomeScreen />`
    - If `isEditorReady`: render `<EditorLayout />`
    - Wrap in a div with `className="h-screen bg-surface text-primary"` and system font stack via Tailwind

    Font stack: The system font stack from UI-SPEC is Tailwind's default `font-sans` — no custom config needed.
  </action>
  <verify>
    <automated>cd /Users/lijunyang/Personal/mysekai-planner && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/components/welcome/WelcomeScreen.tsx contains "MySekai 区域规划器"
    - src/components/welcome/WelcomeScreen.tsx contains "选择区域等级开始编辑"
    - src/components/welcome/WelcomeScreen.tsx renders 5 level cards (iterates AREA_LEVELS)
    - src/components/welcome/WelcomeScreen.tsx calls startEditor on card click
    - src/components/layout/EditorLayout.tsx contains h-10 toolbar area, w-72 sidebar area, h-14 hotbar area, h-8 status bar area
    - src/App.tsx conditionally renders WelcomeScreen or EditorLayout based on isEditorReady
    - `pnpm build` exits with code 0
  </acceptance_criteria>
  <done>Welcome screen shows 5 area level cards. Clicking a card transitions to the editor layout shell. Layout has correct proportions per UI-SPEC.</done>
</task>

<task type="auto">
  <name>Task 2: Toolbar, Hotbar, and Status Bar components</name>
  <files>
    src/components/toolbar/Toolbar.tsx,
    src/components/toolbar/ToolButton.tsx,
    src/components/hotbar/Hotbar.tsx,
    src/components/status/StatusBar.tsx
  </files>
  <read_first>
    src/types/editor.ts (ToolMode, HotbarSlot types),
    .planning/phases/01-foundation-core-editor/01-UI-SPEC.md (Toolbar Buttons table, Hotbar Slots spec, Status Bar in Layout Contract, Copywriting Contract for tooltip text, Keyboard Shortcuts table)
  </read_first>
  <action>
    1. Create `src/components/toolbar/ToolButton.tsx`:

    A reusable toggle button for the toolbar:
    ```typescript
    interface ToolButtonProps {
      icon: React.ElementType;  // lucide icon component
      label: string;            // tooltip text
      isActive: boolean;
      activeClassName?: string; // e.g. "bg-accent text-surface" or "bg-destructive text-white"
      onClick: () => void;
      shortcut?: string;        // e.g. "(V)"
    }
    ```

    Render: `button` with `h-8 w-8 rounded-md flex items-center justify-center` base classes. When active, apply `activeClassName`. When inactive, apply `bg-transparent hover:bg-surface-hover text-primary`. Wrap in Radix Tooltip showing `label`. The icon is rendered at size 18.

    2. Create `src/components/toolbar/Toolbar.tsx`:

    Height `h-10`, `bg-surface-raised`, `border-b border-default`, flex row with items centered vertically, `px-2 gap-1`.

    Left group (tool modes):
    - Select/Move: `MousePointer2` icon, active: `bg-accent text-surface`, tooltip "选择/移动 (V)"
    - Stamp: `Stamp` icon, active: `bg-accent text-surface`, tooltip "放置 (B)"
    - Remove: `Eraser` icon, active: `bg-destructive text-white`, tooltip "删除 (X)"

    Separator: `div` with `w-px h-6 bg-border-default mx-1`

    Overwrite toggle: `Replace` icon, when active: `bg-accent/20 border border-accent`, tooltip "覆盖模式"

    Separator.

    Undo/Redo (action buttons, not toggles):
    - Undo: `Undo2` icon, tooltip "撤销 (Ctrl+Z)". Disabled state with `opacity-50 cursor-not-allowed` when no undo history. onClick calls `useEditorStore.temporal.getState().undo()`
    - Redo: `Redo2` icon, tooltip "重做 (Ctrl+Y)". Similar disabled state. onClick calls `.redo()`

    Right group (pushed to right with `ml-auto`):
    - Area Level dropdown: Radix DropdownMenu. Trigger shows `Map` icon + current level label (e.g. "Lv.3 (70×70)"). Dropdown items: all 5 levels. onClick calls `setAreaLevel(level)`.

    Read `toolMode`, `overwriteEnabled`, `areaLevel`, `gridSize` from editor store.

    3. Create `src/components/hotbar/Hotbar.tsx`:

    Height `h-14`, `bg-surface-raised`, `border-t border-default`, flex row centered, `gap-2`.

    Render 9 slots (numbered 1-9). Each slot:
    - Size: `w-12 h-12 rounded-md bg-surface border border-default`
    - Empty slot: number label in `text-muted text-xs` centered
    - Assigned slot: CDN thumbnail image (scaled to fit) + number badge `absolute top-0 left-0 text-xs bg-black/60 text-white px-1 rounded-br-sm`
    - Active slot (activeFixtureId matches slot's fixtureId): `border-2 border-accent ring-1 ring-accent/30`
    - Hover: `hover:bg-surface-hover cursor-pointer`
    - onClick: call `activateHotbar(slotNumber)` from store

    Read `hotbar`, `activeFixtureId` from editor store.

    4. Create `src/components/status/StatusBar.tsx`:

    Height `h-8`, `bg-surface-raised`, `border-t border-default`, flex row, `px-4 text-xs text-muted items-center gap-4`.

    Three info sections separated by `|` dividers (use `border-l border-default pl-4` on non-first items):
    - "区域等级: {AREA_LEVELS[areaLevel].label} ({gridSize.width}×{gridSize.depth})"
    - "已配置: {Object.keys(placedItems).length}件"
    - "缩放: 100%" (hardcoded for now, will be wired to canvas zoom in Plan 05)

    Read `areaLevel`, `gridSize`, `placedItems` from editor store.
  </action>
  <verify>
    <automated>cd /Users/lijunyang/Personal/mysekai-planner && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/components/toolbar/Toolbar.tsx imports MousePointer2, Stamp, Eraser, Undo2, Redo2, Replace, Map from lucide-react
    - src/components/toolbar/Toolbar.tsx contains "选择/移动" and "放置" and "删除" tooltip strings
    - src/components/toolbar/Toolbar.tsx contains DropdownMenu for area level selection
    - src/components/toolbar/ToolButton.tsx accepts icon, label, isActive, activeClassName, onClick props
    - src/components/hotbar/Hotbar.tsx renders 9 slots (map over array of length 9)
    - src/components/hotbar/Hotbar.tsx shows border-accent on active slot
    - src/components/status/StatusBar.tsx contains "区域等级" and "已配置" and "缩放"
    - `pnpm build` exits with code 0
  </acceptance_criteria>
  <done>Toolbar shows 3 tool mode buttons with correct icons and active states, overwrite toggle, undo/redo buttons, and area level dropdown. Hotbar shows 9 numbered slots with active highlighting. Status bar shows area level, item count, and zoom.</done>
</task>

</tasks>

<verification>
- `pnpm build` exits 0
- App boots to welcome screen with 5 level cards
- Clicking a level card transitions to editor layout
- Toolbar renders all buttons with correct lucide icons
- Hotbar shows 9 slots
- Status bar shows context information
</verification>

<success_criteria>
- Welcome screen displays with title, subtitle, 5 level cards, and prompt text per UI-SPEC
- Editor layout has correct structure: toolbar (h-10) + sidebar (w-72) + canvas (flex-1) + hotbar (h-14) + status bar (h-8)
- Toolbar tool mode buttons toggle active state with correct colors (accent for select/stamp, destructive for remove)
- Area level dropdown works and updates store
- Hotbar renders 9 slots with number labels
- Status bar shows area level, item count, zoom
- All previous tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-core-editor/01-03-SUMMARY.md`
</output>
