---
phase: 09-catalog-overhaul-genre-driven-categories-with-search
plan: 05
type: execute
wave: 3
depends_on: ["09-03", "09-04"]
files_modified:
  - src/data/fixtures.ts
  - src/components/chrome/CLAUDE.md
autonomous: false
requirements: [CATL-05]
must_haves:
  truths:
    - "src/data/fixtures.ts no longer exports Phase7Category type or filterByPhase7Category function (CATL-05)"
    - "All call sites of filterByPhase7Category have been migrated (verified by tsc clean)"
    - "src/components/chrome/CLAUDE.md L2 doc reflects the Phase 9 changes (CatalogRail consumes mainGenres + uses genreIcons.ts)"
    - "Manual UAT confirms the rail renders 15 buttons (1 ALL + 14 outdoor genres), chip strip appears on 一般/小物/植物, hides on ぬいぐるみ/ブロック, search shows breadcrumbs and restores prior selection on clear"
  artifacts:
    - path: src/data/fixtures.ts
      provides: "Phase 9 final shape — heuristic regexes deleted"
    - path: src/components/chrome/CLAUDE.md
      provides: "Updated L2 doc for chrome shell"
  key_links:
    - from: src/data/fixtures.ts
      to: src/data/__tests__/fixtures.test.ts
      via: "negative test (filterByPhase7Category must be undefined)"
      pattern: "^export function filterByPhase7Category"
---

<objective>
Final cleanup wave. Delete `Phase7Category` type union and `filterByPhase7Category` function from `src/data/fixtures.ts` (D-03 — heuristic regexes removed wholesale), turning the CATL-05 negative test stub from RED to GREEN. Update `src/components/chrome/CLAUDE.md` L2 doc for DocOps compliance. Run a human-verify checkpoint to confirm the user-facing flow matches UI-SPEC.

Purpose: CATL-05 final gate (heuristic deletion); DocOps L2 isomorphism per CLAUDE.md (project instructions); UAT to catch any visual/interaction regression that automated tests can't detect (chip-strip slide-in animation, scroll feel, breadcrumb legibility on real tile gradients).

Output: Two-file cleanup + 1 human-verify checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/09-catalog-overhaul-genre-driven-categories-with-search/09-CONTEXT.md
@.planning/phases/09-catalog-overhaul-genre-driven-categories-with-search/09-RESEARCH.md
@.planning/phases/09-catalog-overhaul-genre-driven-categories-with-search/09-UI-SPEC.md
@src/data/fixtures.ts
@src/components/chrome/CLAUDE.md

<interfaces>
After plans 09-03 and 09-04 complete:
- `CatalogRail.tsx` no longer references `Phase7Category` or `filterByPhase7Category`
- `CatalogSidebar.tsx` no longer references `filterByPhase7Category`
- The store no longer imports `Phase7Category` (plan 09-02 widened the field type)

Therefore the only remaining references to `filterByPhase7Category` / `Phase7Category` should be:
1. `src/data/fixtures.ts` (the definitions themselves — to be deleted in this plan)
2. `src/data/__tests__/fixtures.test.ts` (the negative test stub from plan 09-01 — turns GREEN when the symbol disappears)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Delete Phase7Category + filterByPhase7Category from fixtures.ts; update L2 doc</name>
  <files>src/data/fixtures.ts, src/components/chrome/CLAUDE.md</files>
  <read_first>
    - src/data/fixtures.ts (FULL — delete only lines 129-168 region containing `export type Phase7Category` and `export function filterByPhase7Category`; preserve every other export including searchFixtures, filterByGenre, filterOutdoorFixtures, getThumbnailUrl, getItemLayer, getSystemFixtures, getGroundSubtype, isBrushEligible, getBrushInteraction)
    - src/components/chrome/CLAUDE.md (FULL — current L2 doc; needs update for CatalogRail's Phase 9 role: now consumes mainGenres prop and uses genreIcons.ts)
    - .planning/phases/09-catalog-overhaul-genre-driven-categories-with-search/09-UI-SPEC.md (Inheritance Summary §"What Phase 9 deletes" — this list is the deletion contract)
    - CLAUDE.md (project root — DocOps L2 obligations)
  </read_first>
  <behavior>
    - `src/data/fixtures.ts` no longer exports `Phase7Category` or `filterByPhase7Category`
    - `pnpm vitest run src/data/__tests__/fixtures.test.ts` exits 0 (negative test now GREEN)
    - `src/components/chrome/CLAUDE.md` L2 doc has an updated entry for `CatalogRail.tsx` reflecting Phase 9 (consumes `mainGenres`, uses `genreIcons.ts`, vertical scroll)
    - `pnpm build` (`tsc -b && vite build`) exits 0 — no dangling references
  </behavior>
  <action>
    1. **Edit `src/data/fixtures.ts`:**
       - Delete the `export type Phase7Category = ...` union (line ~129)
       - Delete the entire `export function filterByPhase7Category(fixtures, category)` function body (lines ~139–168) including its heuristic regexes (`/植|花|tree|plant|flower/`, `/棚|shelf|rack|display/`, etc. — RESEARCH §"What Phase 9 deletes" makes this explicit)
       - Update the file's L3 header — remove any INPUT/OUTPUT mention of Phase7Category. Add a note `// Phase 9: 删除 Phase7Category + filterByPhase7Category（启发式正则替换为游戏数据驱动 — D-03）` to the changelog comment if one exists.
    2. **Verify call sites are migrated.** Run `grep -rn "filterByPhase7Category\|Phase7Category" src/` — must return ZERO matches outside the negative test stub. If any reference remains, it's a missed migration from plan 09-03 or 09-04 — fix in this plan (scope-creep is acceptable here as it's the cleanup wave).
    3. **Edit `src/components/chrome/CLAUDE.md`:** Update the L2 entry for `CatalogRail.tsx`:
       - INPUT: now consumes `fixtures: Fixture[]` + `mainGenres: FixtureMainGenre[]` (was unused) + `fixtureMap` for category derivation
       - OUTPUT: 72-px rail with 1 ALL button + N curated outdoor mainGenres derived from `mainGenres ∩ presentFixtureMainGenreIds`; lucide icons via `genreIcons.ts`; vertical scroll engaged on overflow
       - POS: unchanged (Slot B, 72px collapsed / part of 320px expanded)
       - Add a new entry for `genreIcons.ts` per DocOps L2 (new file in chrome/): role = `assetbundleName → lucide.ElementType` lookup table for rail buttons.
    4. **Run full test suite + build.**
  </action>
  <verify>
    <automated>pnpm vitest run --reporter=dot && pnpm tsc --noEmit && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "filterByPhase7Category" src/` returns ONLY a single match in `src/data/__tests__/fixtures.test.ts` (the negative test referencing it as a string)
    - `grep -rn "Phase7Category" src/` returns ONLY the negative test reference
    - `src/data/fixtures.ts` does NOT contain `export type Phase7Category` or `export function filterByPhase7Category`
    - `src/data/fixtures.ts` does NOT contain heuristic regex patterns `/植/` `/花/` `/棚/` `/shelf/` (these were inside filterByPhase7Category)
    - `src/components/chrome/CLAUDE.md` mentions `genreIcons.ts`
    - `src/components/chrome/CLAUDE.md` mentions `mainGenres` in CatalogRail's INPUT line
    - `pnpm vitest run` reports all tests GREEN (CATL-05/06/07/08/09/10/11 — the full Phase 9 set)
    - `pnpm tsc --noEmit` exits 0
    - `pnpm build` exits 0
  </acceptance_criteria>
  <done>All 7 phase requirements GREEN; heuristic deletion complete; L2 doc up to date; production build clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human UAT — confirm catalog overhaul matches UI-SPEC</name>
  <read_first>
    - .planning/phases/09-catalog-overhaul-genre-driven-categories-with-search/09-UI-SPEC.md (full document — interaction contract, copywriting contract, animation timings)
    - .planning/phases/09-catalog-overhaul-genre-driven-categories-with-search/09-CONTEXT.md (D-01..D-17 — every locked decision the user signed off on)
  </read_first>
  <what-built>
    Game-data-driven catalog rail (15 buttons: 全部 + 14 outdoor genres) with lucide icons; subGenre chip strip in catalog body when active mainGenre has ≥2 derivable subGenres; search broadens to all genres with breadcrumb pills on result tiles; clearing search restores prior selection; persist v3→v4 migration losslessly resets to 全部.
  </what-built>
  <how-to-verify>
    Run `pnpm dev` and open the editor, then walk through these checks:

    **Rail (CatalogRail) — D-01..D-03, D-09..D-11, CATL-05/06/09**
    1. Confirm 15 rail buttons render: 全部 (Grid3x3 icon), then ぬいぐるみ (Cat), 一般 (Sofa), 小物 (Lamp), ブロック (Box), 植物 (TreePine), 壁掛け (Frame), カラータイル (Grid2x2), ラグ (Square), ディスプレイ (Image), 柵 (Fence), 道 (Route), キャンバス (Palette), 大型 (Trees), その他 (MoreHorizontal). Phase 7's old 8-button heuristic list (棚/植物/ブロック/ディスプレイ heuristic) must NOT appear.
    2. Hover a button → tooltip shows the Japanese name. Click 一般 → button shows the Phase 7 active treatment (sky-gradient 4×28 left bar, sky-tint icon bg, ink #0e3955).
    3. Resize browser height down until the rail content overflows → vertical scroll engages (D-09). Hamburger / grip pinned at top remains visible.
    4. Open DevTools Network tab and reload — confirm ZERO `storage.sekai.best/...icon_*` 404s (UI-SPEC: lucide-only path).

    **Chip strip (CatalogSidebar) — D-05..D-08, CATL-07**
    5. Click 一般 → ≥2 subGenre chips appear above the grid (テーブル, チェア, 棚, 家電, 観葉植物, ベッド, その他 — at least 7 expected per RESEARCH SubGenre Distribution). First chip is 全部.
    6. Click ぬいぐるみ → NO chip strip (only 1 derivable subGenre — UI-SPEC §2 hide<2 rule).
    7. Click ブロック → NO chip strip (subGenre id is null for all blocks).
    8. Click 全部 → NO chip strip (D-07).
    9. While in 一般, click テーブル chip → grid narrows. Click 全部 chip → grid widens to all 一般.
    10. Reload page → chip-strip selection NOT restored (D-08 — in-memory only).

    **Search — D-12..D-15, CATL-10**
    11. With 一般 + テーブル selected, type `テーブル` in search → chip strip hides; result tiles each show a small breadcrumb pill (一般 / ぬいぐるみ / etc.) per UI-SPEC §3.
    12. Clear search (X button or backspace to empty) → 一般 + テーブル restored as the active filters; chip strip reappears.
    13. With search active, click 小物 in the rail → grid does NOT narrow (search still wins, D-13). Clear search → 小物 is now the active mainGenre (the click pre-staged for after-clear).
    14. Type a query that matches no fixtures (e.g. `xxxxxxxxx`) → empty state `該当する家具はありません` + body `検索語またはカテゴリを変更してみてください` shows (CATL-11, D-17).

    **Persist migration — CATL-08**
    15. Open DevTools → Application → Local Storage → `mysekai-planner-design`. Confirm `version: 4` and `state.activeCategory` is either `'all'` or a number.
    16. Hand-edit the localStorage to `{ ..., version: 3, state: { activeCategory: 'shelf', ... } }`, reload → no crash; activeCategory rehydrates to `'all'` and the 全部 rail button is highlighted (D-04, RESEARCH Pitfall §3).

    **No regressions — Phase 7 contract preserved**
    17. Cat-rail collapse / expand still works (Phase 7 hamburger).
    18. Hotbar / floatbar / cost popover / top rail / zoom dock untouched.
    19. Place / move / rotate / delete fixtures still works on the canvas.
  </how-to-verify>
  <resume-signal>Type "approved" to mark Phase 9 complete, OR describe any divergence from UI-SPEC for gap-closure planning.</resume-signal>
  <files>n/a (human-verify checkpoint — no file edits)</files>
  <action>Run `pnpm dev`, then complete the 19-step walkthrough above in <how-to-verify>. Report any divergence; otherwise type "approved".</action>
  <verify>
    <automated>echo "checkpoint:human-verify — awaiting user approval"</automated>
  </verify>
  <done>User types "approved" OR all 19 walkthrough steps pass without divergence; any divergence routes to /gsd:plan-phase --gaps for closure.</done>
</task>

</tasks>

<verification>
- All 7 CATL requirements have GREEN automated tests
- `pnpm build` exits 0
- `grep -rn "filterByPhase7Category\|Phase7Category" src/` returns at most the negative-test reference
- Human UAT signed off
</verification>

<success_criteria>
1. CATL-05 GREEN — heuristic regex deletion confirmed by negative test
2. DocOps L2 isomorphism restored (`src/components/chrome/CLAUDE.md` updated)
3. Production build (`pnpm build`) green
4. UAT signed off — 19-step manual walkthrough confirms UI-SPEC contract
5. No regressions in Phase 7 chrome (rail collapse, hotbar, floatbar, top rail, popover, zoom)
</success_criteria>

<output>
After completion, create `.planning/phases/09-catalog-overhaul-genre-driven-categories-with-search/09-05-SUMMARY.md` listing the deletions, the L2 doc diff, and the UAT outcomes (which steps passed, any divergence flagged for gap-closure).
</output>
